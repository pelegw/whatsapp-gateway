meta {
  name: List contacts
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/contacts?q=&limit=50
  body: none
  auth: bearer
}

auth:bearer {
  token: {{agentKey}}
}

assert {
  res.status: eq 200
}
