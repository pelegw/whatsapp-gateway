meta {
  name: WhatsApp status
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/admin/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

assert {
  res.status: eq 200
}
