meta {
  name: Approve draft
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v1/admin/drafts/{{draftId}}/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

docs {
  Human approval -> actually sends via the sidecar (rate-limited).
}
