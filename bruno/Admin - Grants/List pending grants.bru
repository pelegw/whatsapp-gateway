meta {
  name: List pending grants
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/admin/grants?status=pending
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

assert {
  res.status: eq 200
}
