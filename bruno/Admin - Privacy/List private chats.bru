meta {
  name: List private chats
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/admin/privacy/chats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

docs {
  The global private list: chats NO agent key can read (lists, messages, search, media, events).
}
